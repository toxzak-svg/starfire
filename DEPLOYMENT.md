# Starfire Deployment

The authoritative deployment guide is:

- [`docs/deployment.md`](docs/deployment.md)

Current deployment uses a gated Docker image on Render with persistent state mounted at `/data`. The Next.js client under `ui/` can be deployed separately to Vercel or run locally.

This short file remains as a compatibility pointer for older links.
