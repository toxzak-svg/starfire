{
  "name": "starfire-deployment",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bash scripts/cloudflare-s6c-verify.sh; npm --prefix ui ci && npm --prefix ui run build",
    "build:ui": "npm --prefix ui ci && npm --prefix ui run build",
    "preview:cloudflare": "npx wrangler pages dev ui/out",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy ui/out --project-name starfire"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  }
}
