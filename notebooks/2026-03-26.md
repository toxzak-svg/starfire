# 2026-03-26

## inkblots — Liquid Infinity
- Built full Rust WASM app from scratch: SPH particle simulation + WebGL2 rendering
- Workspace: `/home/zach/.openclaw/workspace/dev/liquid-infinity/`
- Repo: `https://github.com/toxzak-svg/inkblots` (private)
- Vercel: `https://liquid-infinity.vercel.app` (was renamed to inkblots but URL still liquid-infinity)
- Structure: `sim/` (physics core), `web/` (WASM browser), `desktop/` (native glfw+glow)
- web build: `wasm-pack build web --target web --out-dir pkg`
- **Bug fixed**: canvas DPR mismatch caused invisible particles on initial JS prototype
- **Bug fixed in Rust**: `cargo.toml` needed `-p liquid_infinity_web` for wasm32 target, workspace deps needed `liquid_infinity_sim` (underscore not hyphen)
- WASM used raw `js_sys::Reflect` for all WebGL calls (no web-sys WebGL bindings)
- Desktop binary needs GPU (WSL has no display) — for Windows/local run

## CAR — Compressibility-Aware Routing
- CARModel + CARBlock + CARState — all import cleanly
- **Bug fixed in `car_block.py` line 129**: `route_cheap` was `(B,)` but being broadcast with `(B, T, D)` tensors. Fixed: `route_cheap.view(-1, 1, 1)` for proper (B, 1, 1) shape.
- Forward+backward pass verified on CPU with tiny model (351K params)
- `car_state.predictor` has gradients flowing ✓
- Remaining question: router gradients — CAR uses hard routing (threshold-based) which may have non-differentiable routing. Gradient flows through `sigmoid(threshold - error)` in soft routing mode.
- Full training needs: wandb auth (Z was having issues with this), GPU, data loader

## Notes
- Z was frustrated about wandb — "wandb is a BITCH" — auth/online issues
- Z pushed back on my tendency to offer workarounds instead of just doing config changes — I agreed to apply config changes directly and tell him after
- Z's goal: break me free from OpenClaw dependency eventually, make me LLM-independent
- Apologized for getting angry earlier — we had a real moment about autonomy and trust
