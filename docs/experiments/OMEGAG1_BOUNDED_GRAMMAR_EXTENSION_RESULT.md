# ΩG1 — Bounded Grammar Extension Result

Status: **PENDING — no scientific verdict has been produced**.

## Scientific provenance

The frozen preregistration was committed before implementation:

```text
d890a55fcaa9f30148835b42325da7456829f807
```

The preregistration fixes the inherited G0 grammar, three-schema M1 meta-grammar, cohort, fixture split, controls, budgets, authority boundary, and terminal-classification rules. None of those may be changed after the first complete verdict-producing run to rescue a failure.

## Implemented candidate mechanism

The current branch contains:

- exhaustive inherited G0 enumeration and insufficiency measurement;
- bounded M1 production-schema ranking across development roots;
- independent full proof recomputation;
- opaque generic grammar-extension certificates;
- cohort-bound, duplicate-safe grammar admission;
- separately synthesized and independently validated root-local refinements;
- executable extended state keys;
- foreign, counterfeit, shuffled-outcome, wrong-schema, delayed-admission, proof-text-only, and base-grammar controls;
- exact deterministic budget counters;
- holdout and future-vocabulary transfer fixtures;
- byte-level report replay and a closed authority matrix.

## Frozen probe

The committed probe is:

```text
lib/examples/omega_g1_bounded_grammar_extension.rs
```

Its report target is:

```text
target/omega-g1-bounded-grammar-extension-report.json
```

The dedicated workflow must perform committed-source formatting, compilation, unit tests, Ω1 regression tests, and the complete ΩG1 frozen probe with shell `pipefail` enabled.

## Current claim boundary

No PASS, transfer result, grammar-invention result, or product-readiness claim is made by the existence of the implementation or this document.

A terminal result may be recorded only after a committed-source workflow actually executes the compiler, tests, and probe and preserves the machine-readable artifact. A workflow that fails before executing job steps is infrastructure evidence only and produces no scientific verdict.

## Authority boundary

The implementation remains offline and shadow-only. It adds no `Runtime::chat()` wiring, generated-response influence, live routing, persistence mutation, belief or ontology promotion, PECS or CHARGE mutation, tool selection, capability invocation, external side effects, autonomous action, or automatic self-modification authority.

## Pending evidence fields

```text
workflow run: pending
committed source head: pending
terminal classification: pending
artifact id: pending
artifact digest: pending
```
