# ΩG1 — Bounded Grammar Extension Result

Status: **PASS**.

ΩG1 passed its frozen scientific contract on the exact committed source. The result supports bounded production-schema admission and transfer under the preregistered permutation fixture. It does not establish unrestricted grammar invention, natural-language acquisition, live self-modification, autonomous agency, or AGI.

## Scientific provenance

The frozen preregistration was committed before implementation:

```text
d890a55fcaa9f30148835b42325da7456829f807
```

The preregistration fixed the inherited G0 grammar, three-schema M1 meta-grammar, cohort, fixture split, controls, budgets, authority boundary, and terminal-classification rules. None of those were changed after the first complete verdict-producing run.

## Terminal committed-source run

The terminal run tested the GitHub-verified merge commit below without formatting, generation, or any other source rewrite before execution:

```text
058d3f173a7638eff621e7f374614f0383b164ce
```

Execution environment:

```text
Vercel deployment: dpl_Dg3nfpH2DxY4BnhZYp3TFirAok2j
Inspector: https://vercel.com/autostackdev/star/Dg3nfpH2DxY4BnhZYp3TFirAok2j
Final deployment state: READY
```

Committed scientific source hashes:

```text
lib/grammar_extension.rs
sha256 e02cd087146b8b6448110ef71f0ecbde807c5887b0533d7d18371fb032dbe3ba

lib/examples/omega_g1_bounded_grammar_extension.rs
sha256 20596512b8bd3362800ef8cd50a43613098e51d5edf4845b4fc89b49dabcf81f
```

The verifier asserted a pristine checkout before execution and reasserted that both scientific source files remained unchanged after the probe.

## Executed verification

The exact-source run completed all of the following:

- `cargo check -p star --all-targets --locked`;
- `cargo test -p star grammar_extension --locked`;
- `cargo test -p star representation_genesis --locked`;
- `cargo run -p star --example omega1_endogenous_state_space_genesis --locked` with terminal `PASS`;
- `cargo run -p star --example omega_g1_bounded_grammar_extension --locked` with terminal `PASS`;
- machine-report existence and terminal-classification checks;
- byte digest of the machine report;
- source-immutability recheck;
- the normal Next.js production build.

## Frozen result

Winning generic schema:

```text
adjacent_before
```

Transfer result:

- all 8 held-out roots passed;
- all 24 future-vocabulary roots passed;
- `future_thermal`: 8/8 roots;
- `future_software`: 8/8 roots;
- `future_watershed`: 8/8 roots;
- every matched control produced zero correct predictions;
- replay was byte-exact;
- proposal and independent-validation budgets matched exactly;
- root-local refinement budgets matched exactly.

All frozen gates were true:

```text
cohort_exact
split_exact
outcome_coverage_exact
development_budgets_exact
independent_validation_equal_budget
base_grammar_insufficient_everywhere
unique_schema_winner
correct_schema_admitted
duplicate_rejected_atomically
foreign_rejected_atomically
counterfeit_rejected
shuffled_proof_rejected
holdout_transfer_exact
future_transfer_exact
every_control_zero
future_families_exact
local_budgets_exact
source_immutable
authority_closed
replay_exact
```

Terminal classification:

```text
PASS
```

Machine report:

```text
target/omega-g1-bounded-grammar-extension-report.json
sha256 f84a7f304a0c6f11513e41e04603c5009d26e97d962e2fba2db97e67696def83
```

## Authority boundary

The implementation remained offline and shadow-only throughout the run. The report confirmed all authority flags were false:

```text
runtime_chat_wiring
response_influence
routing_authority
persistence_authority
belief_or_ontology_promotion
pecs_or_charge_mutation
tool_or_capability_selection
autonomous_action
```

No live chat path, routing path, persistence path, ontology promotion, tool selection, capability invocation, external side effect, autonomous action, or automatic self-modification authority was added.

## Formatting and infrastructure note

GitHub-hosted Actions created jobs but executed zero steps because the hosted runner lane was unavailable at the account level. Those runs produced no scientific verdict.

A first verified Vercel attempt identified rustfmt normalization differences in the two new Rust files and stopped before scientific execution. A separate diagnostic applied rustfmt only inside an ephemeral checkout and passed the compiler, regressions, controls, and probe. The terminal verdict above was then produced by a new verified run against the exact committed source with no transient source edits. Rustfmt normalization is therefore a repository-style follow-up, not part of this recorded scientific claim; any later formatting-only change must rerun the probe before replacing these source hashes.

## Claim boundary

The supported claim is:

> Bounded production-schema admission under the frozen permutation fixture.

The result does not support claims of unrestricted grammar invention, natural-language grammar induction, live self-modification, autonomous reasoning in an open environment, product readiness, or AGI.
