# H-Infant Multi-Pressure Verdict Confirmation

This child branch exists only to force GitHub Actions to execute the already-frozen multi-pressure prioritization workflow from a base branch that contains the workflow definition.

It changes no experiment code, seeds, controls, scoring rule, or acceptance gate.
