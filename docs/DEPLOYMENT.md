# Deployment Guide Moved

The authoritative Starfire deployment guide is:

- [`deployment.md`](deployment.md)

That guide documents the current Render backend, `/data` persistence, Docker release gates, Vercel-compatible UI, environment variables, verification steps, and troubleshooting.

This uppercase-path file remains only to preserve older links.
